$db_host = "mysql-db-private-endpoint"
$db_user = "admin"
$db_password = "sua_senha_secreta"

file { '/opt/phpmyadmin/.env':
  ensure  => file,
  content => template('phpmyadmin/phpmyadmin-env.erb'),
  require => File['/opt/phpmyadmin'],
}